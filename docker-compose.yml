version: '3'
services:
  pdf-compose-service:
    build:
      context: server
    ports:
        - "8090:8090"
  controller-service:
    build:
      context: controller
    ports:
        - "8080:8080"
